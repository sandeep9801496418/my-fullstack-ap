import React from 'react';

const Home = ()=>{
    return (
        <div>
            <h1>Welcome to the Home Page!</h1>
            <p>This is a full-stack project with authentication and role-based authorization.</p>
        </div>
    );
};

export default Home;
